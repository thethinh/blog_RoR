<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             data: { confirm: "You sure?" }) do |f| %>
  <i class="fas fa-user-check icon-connect-friend"></i>
  <%= f.submit "", class: "btn btn-success" %>
<% end %>

