<%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),
             html: { method: :delete },
             data: { confirm: "You sure?" },
             remote: true) do |f| %>
  <%= f.submit "Waiting recept", class: "btn btn-warning" %>
<% end %>