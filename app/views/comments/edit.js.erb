
$("#<%= @comment.id %>").html("<%= escape_javascript render 'comments/comment_form', 
                                    { url_action: user_comment_path(current_user, @comment),
                                      method_controll: "PATCH",
                                      class_form_cmt: "typecmt #{@comment.micropost.id}" } %>")
