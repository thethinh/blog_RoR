<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <div class="infor-user-feed">
        <section class="user_info">
          <%= render 'shared/user_info' %>
        </section>
      </div>

      <section class="micropost_form">
        <%= render 'shared/micropost_form' %>
      </section>
    </aside>
    <div class="col-md-8">
      <h3 class="title-sum-micropost">Microposts feed</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% end %>